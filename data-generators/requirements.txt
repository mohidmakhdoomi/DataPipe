# Data Generator Requirements
# Python >= 3.9 required (due to pandas 2.1+ requirement)

# Core Dependencies - Required for basic functionality
pandas>=2.1.0,<2.3.0          # Data manipulation and analysis
faker>=18.0.0,<25.0.0          # Generate realistic fake data
kafka-python>=2.0.0,<3.0.0     # Kafka producer functionality
pydantic>=2.0.0,<3.0.0         # Data validation and models
python-dotenv>=1.0.0,<2.0.0    # Environment variable loading

# Performance Dependencies - Optional but recommended for high throughput
# These have fallback mechanisms in the code if not installed
orjson>=3.8.0,<4.0.0           # High-performance JSON serialization (fallback: json)

# Kafka Compression Libraries - Optional performance enhancements
# Install with: pip install kafka-python[snappy,lz4,zstd,crc32c]
python-snappy>=0.6.0,<1.0.0    # Snappy compression for Kafka
lz4>=4.0.0,<5.0.0              # LZ4 compression for Kafka
zstandard>=0.19.0,<1.0.0       # Zstandard compression for Kafka
crc32c>=2.0.0,<3.0.0           # CRC32C checksums for Kafka performance

# Installation Instructions:
# Basic: pip install -r requirements.txt
# The application will work with just the core dependencies
# Performance libraries will be used automatically if available